---
description: Recall durable facts from the team's know brain
argument-hint: [what to look up]
---
Search the team's shared **know** brain for: $ARGUMENTS

Call the `recall` tool (the know connector) with that as the query, then answer using what it returns. If recall finds nothing or only low-confidence matches, say so plainly rather than guessing — the brain may simply not know it yet. If a result is flagged DISPUTED, surface that prominently.
