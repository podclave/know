---
description: Resolve an open contradiction in the know brain
argument-hint: [contradiction id or topic, optional]
---
Help me resolve a contradiction in the team **know** brain.

If "$ARGUMENTS" identifies a specific dispute, target that one; otherwise call `contradictions` first and show me the open list so I can pick one.

To carry out my decision, call the `resolve` tool: `keep` if the existing curated fact is correct (the conflicting claim was wrong), or `replace` with the corrected fact text I provide. The decision is mine — never guess it; ask me. The resolution is attributed to me and archived as an audit trail.
