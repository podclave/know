---
description: List open contradictions (disputed facts) in the know brain
---
Call the know brain's `contradictions` tool to list every OPEN dispute — facts where a newer claim conflicts with what's already curated.

Present them clearly. For each, offer to resolve it with me right here: `keep` (the existing curated fact is right and the conflicting claim was wrong) or `replace` (update the fact with a correction I give you). When I decide, carry it out with the `resolve` tool. Don't guess a decision — ask me.
